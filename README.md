# xml-to-csv-data-pipeline
A Python-based data pipeline that extracts, processes, and transforms financial instrument data from XML sources into structured CSV format, with emphasis on validation, testing and clean architecture principles.
